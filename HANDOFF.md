# HANDOFF.md

このファイルは、別のAI（Claude Code 以外も含む）に作業を引き継ぐためのメモ。
作業を始める前に **CLAUDE.md → docs/SPEC.md → このファイル** の順に読むこと。

## プロジェクト

BAI Tool — ブルーアーカイブ総力戦TLをスマホ片手で速記するための単一HTMLツール。
詳細は [CLAUDE.md](CLAUDE.md) と [docs/SPEC.md](docs/SPEC.md)（実装の正典）を参照。

## 直近の作業（このセッション）

### 強化: TL取込パース精度（既知の限界3点のうち2点を解消）

前セッションで実装された `parseTLImportText` の既知の限界を解消した。

1. **「5 チャージ①」のようなキャラ名省略行に対応**
   - ロスター名と一致しないセグメントが来た場合、直前に配置したキャラ
     （`lastChar` で追跡）のスキル行とみなして自動配置するようにした。
   - 末尾に丸数字（①②等）が付いていれば `badge` としても拾う
     （例: `チャージ①` → 直前キャラのセルに `badge:1`）。
2. **「アリスチャージ①,②」のような名前+丸数字の複数指定に対応**
   - 名前の後ろに丸数字が複数付く場合、そのキャラのセルを丸数字の数だけ
     複製し、各セルの `badge` に半角数字（1,2,...）を割り当てるようにした
     （丸数字→数値変換は `circledToNum()` を新設）。
   - カンマ区切りで丸数字だけのトークン（`アリスチャージ①,②` の `②` 部分）が
     単独で来た場合も、直前のセルと同じキャラの別バッジとして追加する。
3. **`〆`（おしまい印）を削除せず保持**
   - 旧実装は `body.replace(/[〆]$/,"")` で単に捨てていたため情報が消えていた。
     `row.memo` に `〆` という文字列として残すよう変更。
4. **注意書き行（※★・等で始まる行）の誤爆を防止**
   - 1の自動配置ルールを入れたことで、`※アリスダメージ足りなそうなら` のような
     プレイヤー向け注釈行にまでキャラが誤配置される副作用が出たため、
     `NOTE_PREFIXES`（※★・#注メモ等）で始まる行は自動配置の対象外にした。
   - さらに、注釈行の次に**字下げ（行頭スペース）された続き行**も同じ注釈の
     一部とみなし、同様に自動配置をスキップするようにした
     （複数行にわたる注釈の2行目以降に誤ってキャラが置かれるのを防止）。
- **残っている既知の限界**: ロスター未登録のキャラ名は引き続き自動解決できない
  （存在しないキャラを推測で作れないため）。事前にロスターへ手動登録しておく
  運用は変わらず前提。`unmatched` 警告で一覧表示はされる。
- 変更箇所は `index.html` 内 `/* ===== TL取込 */` ブロックのみ。
  `parseTLImportText` とその周辺ヘルパー（`splitMarker`/`findRosterChar`等）を
  書き換え、`circledToNum`/`splitNameAndBadges`/`isNoteLine` を新設。
  シート側のUI（`openImportSheet`内のHTML/イベント）には触れていない。
- 確認方法: Playwrightで実ブラウザ実行し、`parseTLImportText` の単体動作・
  取込シートUIでのプレビュー〜反映までの一連の流れ・badge描画（`.cbadge`）を
  目視確認済み。375px幅でのスクリーンショットも確認済み。

### 追加: 元に戻す/やり直し（Undo/Redo）（前々回セッション）

- ヘッダー右上に `↶`(元に戻す) / `↷`(やり直し) ボタンを追加。
- 仕組み: `snapshotHistory()` を行・セル操作系のボタン onclick の先頭、および
  文字入力欄（コスト/タイム/メモ/タイトル）の `focus` 時に呼び、
  `{title, roster, rows, activeId}` の JSON スナップショットを `undoStack` に積む。
  `undo()`/`redo()` で `undoStack`/`redoStack` 間をやり取りし `applySnapshot()` で復元。
  履歴上限は50件（`HISTORY_LIMIT`）。
- 対象は表示中の TL（`state`＝doc）単位。`switchDoc()` で TL を切り替えると
  `resetHistory()` で履歴をクリアする（別 TL の履歴を持ち越さない）。
- 文字入力は1文字ごとに記録すると使い物にならないため、`oninput` ではなく
  `focus`（編集開始前のタイミング）で1回だけ記録する設計にしている。
- `render()` 内で `updateUndoRedoButtons()` を呼び、履歴が空の側のボタンを
  `disabled` にしている。
- TL取込の「解析→反映」ボタンも `snapshotHistory()` 済み（取込前に戻せる）。

### 追加: PinP用余白トグル（前々回セッション）

- ヘッダーに `⬓` ボタンを追加。タップで `#pinpSpacer`（ヘッダー直下の空div）の
  高さを 0 ⇔ 300px に切り替える。
- 背景: ユーザーがスマホの PinP（ピクチャインピクチャ）で参考動画を画面上部に
  重ねながら TL を記録する運用のため、PinP表示中だけ表の先頭行を動画の下に
  隠れないよう下げたい、という要望。常時固定余白ではなくワンタップ切替式を採用。
- 状態は `localStorage`（キー `bai-tool-pinp-gap`）に保存。TLデータ
  （`bai-tool-store-v1`）とは別キーで、端末の表示設定として独立させている。
  リロードしても直前の表示状態を復元する。
- `docs/SPEC.md` の画面レイアウト図・機能要件にも追記済み。

### 修正: 行追加連打でスクロール不能になるバグ（前々回セッション）

- **症状**: 「＋行」ボタンを連続でタップすると、画面が自動スクロールを繰り返して
  操作を受け付けなくなる（入力不可能になる）。
- **原因**:
  `index.html` の `btnAddRow` の `onclick` 内で、行追加のたびに
  `renderTable()`（テーブル全体を作り直す重い処理）→
  `scrollIntoView({behavior:"smooth"})`（アニメーション付きスクロール）
  を実行していた。連打すると、前のスムーズスクロールのアニメーションが終わる前に
  次のスクロール要求が割り込み、スクロール位置の競合が起きてブラウザ側の
  スクロール処理が暴走 → 指のタップ座標とスクロール位置がズレ続けてタップが
  効かなくなる、という流れ。
- **修正内容**:
  - `scrollIntoView` の `behavior` を `"smooth"` → `"instant"`（瞬間移動）に変更。
  - 加えて `scrollRowIntoView()` という関数を新設し、`requestAnimationFrame` で
    スクロール実行を1フレームに1回だけにまとめるようにした（連打時の多重実行防止）。
  - 変更箇所は `index.html` 内、`/* ===== row ops ===== */` コメント直後、
    `btnAddRow` の `onclick` 周辺のみ。他の機能・他のボタンには触れていない。
- **確認方法**: 「＋行」を連打しても画面が暴れずに最終行までジャンプし、
  直後にタップ・入力操作が即座に効くこと。スマホ幅375pxで確認すること。

- ヘッダー右上に `↶`(元に戻す) / `↷`(やり直し) ボタンを追加。
- 仕組み: `snapshotHistory()` を行・セル操作系のボタン onclick の先頭、および
  文字入力欄（コスト/タイム/メモ/タイトル）の `focus` 時に呼び、
  `{title, roster, rows, activeId}` の JSON スナップショットを `undoStack` に積む。
  `undo()`/`redo()` で `undoStack`/`redoStack` 間をやり取りし `applySnapshot()` で復元。
  履歴上限は50件（`HISTORY_LIMIT`）。
- 対象は表示中の TL（`state`＝doc）単位。`switchDoc()` で TL を切り替えると
  `resetHistory()` で履歴をクリアする（別 TL の履歴を持ち越さない）。
- 文字入力は1文字ごとに記録すると使い物にならないため、`oninput` ではなく
  `focus`（編集開始前のタイミング）で1回だけ記録する設計にしている。
- `render()` 内で `updateUndoRedoButtons()` を呼び、履歴が空の側のボタンを
  `disabled` にしている。

### 追加: PinP用余白トグル

- ヘッダーに `⬓` ボタンを追加。タップで `#pinpSpacer`（ヘッダー直下の空div）の
  高さを 0 ⇔ 300px に切り替える。
- 背景: ユーザーがスマホの PinP（ピクチャインピクチャ）で参考動画を画面上部に
  重ねながら TL を記録する運用のため、PinP表示中だけ表の先頭行を動画の下に
  隠れないよう下げたい、という要望。常時固定余白ではなくワンタップ切替式を採用。
- 状態は `localStorage`（キー `bai-tool-pinp-gap`）に保存。TLデータ
  （`bai-tool-store-v1`）とは別キーで、端末の表示設定として独立させている。
  リロードしても直前の表示状態を復元する。
- `docs/SPEC.md` の画面レイアウト図・機能要件にも追記済み。

### 修正: 行追加連打でスクロール不能になるバグ（前回セッション）

- **症状**: 「＋行」ボタンを連続でタップすると、画面が自動スクロールを繰り返して
  操作を受け付けなくなる（入力不可能になる）。
- **原因**:
  `index.html` の `btnAddRow` の `onclick` 内で、行追加のたびに
  `renderTable()`（テーブル全体を作り直す重い処理）→
  `scrollIntoView({behavior:"smooth"})`（アニメーション付きスクロール）
  を実行していた。連打すると、前のスムーズスクロールのアニメーションが終わる前に
  次のスクロール要求が割り込み、スクロール位置の競合が起きてブラウザ側の
  スクロール処理が暴走 → 指のタップ座標とスクロール位置がズレ続けてタップが
  効かなくなる、という流れ。
- **修正内容**:
  - `scrollIntoView` の `behavior` を `"smooth"` → `"instant"`（瞬間移動）に変更。
  - 加えて `scrollRowIntoView()` という関数を新設し、`requestAnimationFrame` で
    スクロール実行を1フレームに1回だけにまとめるようにした（連打時の多重実行防止）。
  - 変更箇所は `index.html` 内、`/* ===== row ops ===== */` コメント直後、
    `btnAddRow` の `onclick` 周辺のみ。他の機能・他のボタンには触れていない。
- **確認方法**: 「＋行」を連打しても画面が暴れずに最終行までジャンプし、
  直後にタップ・入力操作が即座に効くこと。スマホ幅375pxで確認すること。

## 変更してはいけないもの（再掲・最重要）

[CLAUDE.md](CLAUDE.md) にある通り、以下は **本ツールの中核機能** であり、
仕様変更・リファクタの際も弱める/無効化する変更は禁止。変更が必要な場合は
実装前に必ずユーザーに確認を取ること。

1. **キャラ名/アイコンの月1自動更新**
   （GitHub Actions + `scripts/update-chars.mjs`、SchaleDB `data/jp/students.min.json` から取得）
2. **名前選択時のアイコン自動取得**
   （`iconUrlFor` / `autoIcon` / `imageUrlToDataUrl`。取得先は SchaleDB、保存先は端末の
   localStorage のみ。リポジトリに同梱・再配布はしない）

`CHAR_NAMES:START`〜`END`、`CHAR_ICONS:START`〜`END` のマーカーも壊さないこと。

## このプロジェクト共通のコーディング方針

- 単一 `index.html` のみで完結。新規ファイルに分割しない。
- 外部ライブラリ・CDN を追加しない（バニラJS）。
- 対象環境: iPhone Safari（iOS 15+）、スマホ幅375px。**必ずこの幅で見た目を確認する**。
- コメントは日本語。
- 確認コマンド（構文チェックのみ。実行はしない）:
  ```
  node -e "new Function(require('fs').readFileSync('index.html','utf8').match(/<script>([\s\S]*)<\/script>/)[1])"
  ```
- 仕様変更をしたら `docs/SPEC.md` も合わせて更新し、正典を保つこと。
- 既存機能を壊した場合・未指示の変更を加えた場合、ユーザーは強く不満を示す。
  **指示された範囲だけを過不足なく直すこと。**

## ユーザーの作業スタイル（重要）

- 説明は簡潔に。褒め言葉や前置きの雑談は不要。淡々とタスクを進めること。
- 「修正してください」と言われたら、原因特定→修正→構文確認まで一気にやる。
  途中で不要な確認を挟まない（ただし仕様に影響する判断が必要な場合は確認する）。
- 作業のたびに、このHANDOFF.mdを更新した状態でZIPとして渡す運用にしている
  （複数のAIで作業を引き継ぐため）。次にこのプロジェクトを触るAIは、
  ZIPを展開したら必ずこのファイルを最初に読むこと。

## 未着手・既知の懸念（あれば次回ここに追記）

- TL取込の実機（iPhone Safari）でのOCR動作・速度・精度は依然未検証。
  次回触る場合は要確認。
- TL取込の「ロスター未登録キャラ名」は引き続き自動解決できない（仕様上の制約。
  事前にロスターへ手動登録しておく運用が前提）。
- 「画像読み込み」の対象は、IMG_2986.png のようなキャラアイコン並びの
  完成形レイアウト画像（参考にしている出力イメージ）ではなく、
  Discord/X等のテキスト・テキストスクショであることをユーザーに確認済み
  （2026-06）。アイコン画像レイアウトそのものの自動認識は対象外。
