# HANDOFF.md

このファイルは、別のAI（Claude Code 以外も含む）に作業を引き継ぐためのメモ。
作業を始める前に **CLAUDE.md → docs/SPEC.md → このファイル** の順に読むこと。

## プロジェクト

BAI Tool — ブルーアーカイブ総力戦TLをスマホ片手で速記するための単一HTMLツール。
詳細は [CLAUDE.md](CLAUDE.md) と [docs/SPEC.md](docs/SPEC.md)（実装の正典）を参照。

---

## このセッションで行った作業: 全体リファクタリング（バグ発見・修正）

指示: 「全体リファクタリング。問題を見つけたら改善」。index.html 全1282行と
docs/SPEC.md・CLAUDE.md を通読し、以下の問題を発見・修正した。

### 🔴 重大バグ: findRosterChar が「キャラ名＋接尾語」を照合できていなかった

**症状**: ロスターに「アリス（臨戦）」のみ登録した状態で `アリスチャージ①` と
入力しても、一度もアリスにマッチしていなかった（`splitNameAndBadges` が常に `null` を返す）。

**原因**: `findRosterChar` のステップ3（前方一致）は
`"アリスチャージ①".startsWith("アリス（臨戦）")` を見ており、
括弧付きのフル名を前方一致させようとしていたため常に false になっていた。
ステップ4（ベース名1件確定）も「入力名全体」からベース名を切り出す方向で、
`"アリスチャージ①"` からは括弧が無いため何も切り出せず機能していなかった。

**修正**: `findRosterChar` にステップ5を追加。
「ロスター側キャラのベース名（括弧前）が入力の先頭と一致するか」を見る、
逆方向の判定を追加した。これによりベース名一致するロスターキャラが1件だけなら
確定マッチする（例: ロスターに「アリス（臨戦）」のみ→「アリスチャージ①」で確定）。

`splitNameAndBadges` 側も、`c.name.length` で一律に残り文字列を切り出していたため
ステップ5でマッチした場合に文字化けする問題があったので、
「完全一致/前方一致ならフル名の長さ、ベース名一致ならベース名の長さ」を
判定して切り出すよう修正（`matchLen` 変数）。

**影響**: これはユーザーが以前から報告していた「アリスチャージ①がコピーやbadgeを
正しく持てない」系の不具合の根本原因だった可能性が高い。今後、接尾語付き
入力（スキル名・チャージ等）のマッチング精度が改善するはず。要実機確認。

### 🟡 splitNameAndBadges の重複ロジックを統合

**問題**: コピープレフィックス（C/©等）の有無で2つの似た分岐が並存し、
プレフィックスなし側でのフォールバック（`markLabel2`）に `copyPrefix` が
含まれておらず、特殊ケースでコピーラベルが消えるバグがあった。

**修正**: 1本の処理フローに統合。プレフィックス除去後にマッチしなければ、
除去前の原文で再試行し、原文でマッチした場合は `copyPrefix=null` とする
（＝"C"はキャラ名の一部だった、という判定）。

### 🟢 軽微な修正

1. **コメント誤り**: `renderCells`・PNG出力の両方で「コスト: 先頭セルのみ**右下**に
   橙色バッジ」というコメントが、実装（左下）と食い違っていた。コメントを実装に合わせて修正。
2. **`nextNum()` の上限がハードコード10のまま**: ロスター上限を20に拡張した際に
   このヘルパー関数だけ更新漏れがあり、11体目以降のキャラ追加で空き番号検索が
   正しく機能しなかった（`for(let i=1;i<=10;i++)` → `<=20` に修正）。
3. **CSS重複セレクタ統合**: `.trow .c-time input` が2つのルールに分かれていたのを1つに統合。

### 📄 ドキュメントの正典性を修正（実装との乖離を解消）

`docs/SPEC.md` と `CLAUDE.md`、`index.html` 冒頭コメントが、過去セッションの
「コスト列廃止」「ロスター上限20」「cell.mark拡張」等の変更に追従しておらず、
古い仕様（ロスター最大10、コスト独立列、`mark:"←"`固定）のままだった。
以下を実装に合わせて更新：

- データモデルの `char` コメント: `最大10` → `最大20`
- データモデルの `cell.mark`: `("←"|null)` → `(文字列|null)`（実際は"←"以外にチャージ略記等も入る）
- 画面レイアウト図: コスト独立列を削除し、コストが先頭セル左下バッジである旨を明記
- 機能要件「3. 表の編集」: コスト欄の説明を「独立列は無い、先頭セル左下バッジ」に修正
- 機能要件「4. キャラ配置」: セルジェスチャの「左=削除」を実装通り「左=←マークトグル」に修正、
  セル上のバッジ配置（左下=コスト/右下=badge/左上=mark）を追記
- キャラ名マッチングの説明にステップ5（ベース名の先頭一致）を追記

---

## 過去セッションの変更（参照用サマリー）

### コスト列廃止・キャラ左下バッジ化（前セッション）
- 左側の独立コスト列を削除。`.c-num` CSSも削除。
- 行の `row.num` は維持（データ・TL取込は従来通り）。UIでは先頭セルの左下に
  橙色 `.ccost` バッジとして表示。小数点はそのまま表示可能。
- ロスター上限を10→20に拡張。
- PNG出力もレイアウト調整済み。

### TL取込パースの多数の改善（複数セッション）
- 丸数字をコスト数字として認識（`splitMarker`・`zenkakuToNum`）。
- スキル名のみ行（「チャージ①」等）は新規行を作らず直前行にセル追加。
- `※`をNOTE_PREFIXESから除外しMARK_TOKENSで処理。
- あだ名辞書（`bai-tool-nicknames`）・スキルキーワード辞書（`bai-tool-skill-keywords`）。
- 後掛の自動←マーク付与、マーカー略記のアイコン左上表示、NS接尾扱い。
- 行中複数マーカーでのサブセグメント分割（`splitBodyIntoSegments`）。

---

## localStorage キー一覧

| キー | 内容 |
|---|---|
| `bai-tool-store-v1` | TLデータ本体（docs, roster, rows等） |
| `bai-tool-pinp-gap` | PinP余白トグルの状態 |
| `bai-tool-nicknames` | あだ名辞書 |
| `bai-tool-skill-keywords` | スキルキーワード辞書 |

---

## 変更してはいけないもの（最重要）

1. **キャラ名/アイコンの月1自動更新**（GitHub Actions + `scripts/update-chars.mjs`）
2. **名前選択時のアイコン自動取得**（`iconUrlFor` / `autoIcon` / `imageUrlToDataUrl`）

`CHAR_NAMES:START`〜`END`、`CHAR_ICONS:START`〜`END` のマーカーを壊さないこと。

---

## コーディング方針

- 単一 `index.html` のみ。分割・外部ライブラリ追加しない。
- 対象環境: iPhone Safari（iOS 15+）、スマホ幅375px。
- コメントは日本語。
- 構文確認: `node -e "new Function(require('fs').readFileSync('index.html','utf8').match(/<script>([\s\S]*)<\/script>/)[1])"`
- 仕様変更したら `docs/SPEC.md` も更新して正典を保つ（このセッションで乖離を一度解消したので、
  今後は変更のたびに追従させること）。
- 指示された範囲だけを過不足なく直すこと。ただし今回のような「全体リファクタリング」
  指示時は、根本原因を追うバグ修正・ドキュメント整合性の回復まで含めてよい。
- 修正は細切れにせず一括で行うこと。
- 毎回 HANDOFF.md を更新してから ZIP 出力すること（これを怠って強く指摘されたことがある）。

## 未解決・既知の懸念

- 今回の `findRosterChar` ステップ5追加は、テストコード上では正しく動作を確認したが
  **実機（iPhone Safari）での動作は未検証**。特に既存の「アリスチャージ①」的な
  入力を今まで運用していた場合、挙動が変わる（今まで未配置だったのが配置されるようになる）
  ため、実際のTL入力で一度動作確認することを推奨。
- コスト手動編集UIが無い状態が継続。TL取込経由のみコスト入力可能（前セッションからの既知事項）。
- OCR動作は引き続き未検証。
